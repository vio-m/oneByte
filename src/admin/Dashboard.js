import { Card, CardContent, CardHeader } from "@mui/material";

export const Dashboard = () => (
  <Card>
    <CardHeader title="Welcome to the administration panel" />
    <CardContent>May the Force be with you</CardContent>
  </Card>
);